# Ingestion Service Placeholder

Фаза 0 фиксирует только место для ingestion-пайплайна.
Реализация начинается не раньше Фазы 5 и должна оставаться прозрачной относительно `master-spec.md`.

Ограничения:
- не переносить основную оркестрацию из n8n в сервис;
- не вводить скрытый runtime-слой;
- все контракты и версии snapshot должны быть traceable.
