# Adapters Placeholder

Каталог зарезервирован для явно специфицированных адаптеров.

Правила:
- Lead Target API остаётся первым write-контуром;
- CRM adapter не должен обходить Lead Target API напрямую;
- любые новые адаптеры требуют явной привязки к master-spec и phase backlog.
